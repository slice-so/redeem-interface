import { Container, CreateRedeemForm } from "@components/ui"

export default function Create() {
  return (
    <Container page={true} size="max-w-screen-sm">
      <main>
        <CreateRedeemForm />
      </main>
    </Container>
  )
}
